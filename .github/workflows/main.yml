name: MarmitechCICD
on: [push]
  workflow_dispatch:
  
- name: Install dependencies
  uses: actions/npm@v2
  with:
    args: install

- name: Build project
  uses: actions/npm@v2
  with:
    args: quasar build
