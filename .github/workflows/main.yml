name: MarmitechCICD
on:
  push:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Instalando dependências
        with:
          node-version: 16
        run:
          npm install
          quasar build